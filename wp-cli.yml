# This is a location where WordPress core files are installed
path: "public/wp"
user: null
core update:
  locale: "en_US"
core download:
  locale: "en_US"
  # Use this to lock the WordPress version
  version: 6.8.1
disabled_commands:
  - db drop
  - plugin install
